sudo : required
addons:
  chrome: stable
language : node_js
node_js :
 - stable
install:
 - npm install
script:
 - ng test lib-documentations --watch=false --progress=false
 - ng test documentations --watch=false --progress=false

before_script:
  - npm install -g @angular/cli