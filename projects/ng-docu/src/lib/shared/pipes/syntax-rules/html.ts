import { colors } from './colors';
import { SyntaxConfig } from '../../../models/syntax-config';

export const html: SyntaxConfig = {
  lang: 'html',
  keywords: {
  },
  strings: colors.red,
  comments: colors.grey,
  tags: []
};
